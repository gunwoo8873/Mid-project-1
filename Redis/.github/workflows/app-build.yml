name: Node package
on:
  push:
    branches: [ "main", "dev/**", "releas/**", "feature/**", "hotfix/**" ]
  release:
    types: [ "created" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v2
      
      - name: Node Serup
        uses: actions
  
  